apiVersion: v1
kind: Service
metadata:
  name: {{ include "levis_nginx.fullname" . }}
  labels:
    {{- include "levis_nginx.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "levis_nginx.selectorLabels" . | nindent 4 }}
